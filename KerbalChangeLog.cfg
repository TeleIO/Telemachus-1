KERBALCHANGELOG //Required to have this name
{
  showChangelog = True //To show the changelog, this must be set to True
  modName = Telemachus //Add your mod's name here
  VERSION //Declares a version node
  {
    version = 1.6.22 //Version number, numbers only with no spaces!
    change = Added ChangeLog
  }
  VERSION //Declares a version node
  {
    version = 1.7.0 //Version number, numbers only with no spaces!
    change = Fixed HTML/JS Errors; New design; New antennas; Rebuild for KSPv1.7.*
  }
  VERSION
  {
    version = 1.8.0
    change = Minimum KSP version raised to 1.8.0 (max tested 1.12.5)
    change = Fixed camera capture and PartLoader crash on KSP 1.8+
    change = Per-instance antenna state; multiple antennae no longer interfere
    change = Added stock and JNSQ map layers with switching
  }
  VERSION
  {
    version = 1.9.0
    change = Added 177 telemetry endpoints: delta-v (dv.*), alarms (alarm.*), 80+ vessel (v.*) and 30+ orbital (o.*) values
  }
  VERSION
  {
    version = 1.10.0
    change = Mod integrations: FAR, RealChute, VesselView, Astrogator, Principia, Kerbalism
    change = Migrated to SDK-style project; added OpenAPI docs site
    change = Fixed camera memory leak and singleton NullRef
  }
  VERSION
  {
    version = 1.11.0
    change = Added quick save, load, and revert API endpoints
    change = Added API value scaling for embedded controllers
    change = Fixed HTTP/1.1 Connection close handling for embedded clients
  }
  VERSION
  {
    version = 1.12.0
    change = Added CORS allowlist (ALLOWED_ORIGINS) for cross-origin browser dashboards
    change = Action handlers now run on the main Unity thread
    change = Build script portability fixes for macOS/BSD
  }
}
