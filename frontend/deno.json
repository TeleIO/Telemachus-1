{
  "//": "Deno-driven build for the bundled Telemachus web UI. Mirrors the docs/ setup.",
  "nodeModulesDir": "auto",
  "tasks": {
    "gen": "deno run -A scripts/gen-schema.ts",
    "dev": "deno run -A --node-modules-dir npm:vite",
    "build": "deno run -A scripts/gen-schema.ts && deno run -A --node-modules-dir npm:vite build",
    "preview": "deno run -A --node-modules-dir npm:vite preview"
  }
}
